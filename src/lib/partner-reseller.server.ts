/**
 * Reexporta as regras puras da revenda (que também rodam no navegador).
 * A lógica vive em `partner-reseller-rules.ts` para poder ser usada na tela.
 */
export * from "@/lib/partner-reseller-rules";
